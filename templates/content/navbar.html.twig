<nav class="navbar navbar-expand-lg navbar-dark position-fixed w-100 nav_10000">
    <a class="navbar-brand" href="#">
        <img src="{{ asset('build/img/logo/logo.png') }}" alt="" class="logo_top">
        Getting Out Again</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse row" id="navbarSupportedContent">
        <div class="col-3">
            <ul class="navbar-nav mr-auto row">
                <li class="nav-item col-3 text-center">
                    <a class="nav-link" href="{{ path('home')}}">
                        <svg class="icon-grid">
                            <use xlink:href="#svg-grid"></use>
                        </svg>
                    </a>
                </li>
                <li class="nav-item col-3 text-center">
                    <a class="nav-link" href="{{ path('home')}}">Home</a>
                </li>
                <li class="nav-item col-3 text-center">
                    <a class="nav-link {% if current_menu is defined and current_menu == 'profil' %}active{% endif %}" href="{{ path('user.profil') }}">Profil</a>
                </li>
                <li class="nav-item dropdown col-3 text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        ...
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path('etablissements.getAllEtablissement')}}">Etablissements</a>
                        <a class="dropdown-item" href="{{ path('groups.allGroupePublic')}}">Groupes</a>
                        <a class="dropdown-item" href="{{ path('badges.allBadges')}}">Badges</a>
                        <a class="dropdown-item" href="{{ path('events.allEvents')}}">Events</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">A Propos</a>
                    </div>
                </li>
            </ul>
        </div>

        <div class="col-5 search_Bar">
            <form class="form-inline my-2 my-lg-0 row m-0">
                <input class=" col-11" type="search" placeholder="Search" aria-label="Search" id="search"  data-role="searchinput">
                <button class="col-1 p-0" type="submit">
                    <svg class="icon-magnifying-glass">
                        <use xlink:href="#svg-magnifying-glass"></use>
                    </svg>
                </button>
            </form>
            <div id="search_Bar_Result" class="p-4 none">
                <button class="btn_Close">X</button>
                <ul id="search_Bar_Result_Info"></ul>
            </div>
        </div>
        <script>



        </script>
        <div class="col-1">
            lvl user
        </div>
        <div class="col-3">
            <ul class="navbar-nav mr-auto row">
                <li class="nav-item dropdown col-2 text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icons_Hover icon-shopping-bag">
                            <use xlink:href="#svg-shopping-bag"></use>
                        </svg>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

                    </div>
                </li>
                <li class="nav-item dropdown col-2 text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icons_Hover icon-friend">
                            <use xlink:href="#svg-friend"></use>
                        </svg>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

                    </div>
                </li>
                <li class="nav-item dropdown col-2 text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icons_Hover icon-messages">
                            <use xlink:href="#svg-messages"></use>
                        </svg>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">

                    </div>
                </li>
                <li class="nav-item dropdown col-2 text-center">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icons_Hover icon-notification">
                            <use xlink:href="#svg-notification"></use>
                        </svg>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
*
                    </div>
                </li>
                <li class="nav-item dropdown col-4 text-right">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <svg class="icon-settings">
                            <use xlink:href="#svg-settings"></use>
                        </svg>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <div class="dropdown-divider"></div>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                            <p>{{ app.user.pseudo }}</p>
                        <a class="dropdown-item" href="{{ path('user.edit',{'id':app.user.id})}}">Edit Profil</a>
                        <a class="dropdown-item" href="{{ path('groups.create')}}">Créer un groupe</a>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('badges.createBadges') }}">Créer un badge</a>
                        {% endif %}
                        <div class="dropdown-divider"></div>
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
                        {% else %}
                            <a class="dropdown-item text-success" href="{{ path('app_login') }}">Connexion</a>
                        {% endif %}
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>