{% extends 'base.html.twig' %}

{% block body %}
    <div class="p-4">
        {{ form_start(form) }}
        <input type="submit" value="Editer">
        {{ form_end(form) }}
    </div>

{% endblock %}

