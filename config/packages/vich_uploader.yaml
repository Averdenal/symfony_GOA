vich_uploader:
    db_driver: orm

    mappings:
        badges:
            uri_prefix: /img/badges
            upload_destination: '%kernel.project_dir%/assets/img/badges'

        media:
            uri_prefix: /img/media
            upload_destination: '%kernel.project_dir%/assets/img/media'
    #mappings:
    #    products:
    #        uri_prefix: /images/products
    #        upload_destination: '%kernel.project_dir%/public/images/products'
